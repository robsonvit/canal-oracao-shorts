# Dependências Python — Canal Oração Shorts

# IA — geração de texto e transcrição
groq>=0.9.0

# TTS — síntese de voz masculina
kokoro>=0.9.4
soundfile>=0.12.0
# HTTP — download de vídeos e músicas
requests>=2.31.0

# YouTube Data API v3
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
